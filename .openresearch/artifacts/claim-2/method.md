# Claim 2 method

The verifier expands the four table rows into 16 individual cells, records the
claimed interval beside every value, and evaluates membership without rounding
or aggregation. A synthetic table whose LOGDIFF cells are 96% and constant
cells are 70% is the negative control: the falsification verifier must reject
that range-conforming input.
