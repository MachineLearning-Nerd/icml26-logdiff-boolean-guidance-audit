# Approach ledger — LOGDIFF perfect-score campaign

The current official verdict awards 6/10 at Space revision
`3ecc5f9d8c3771267e2a1715a7ab4b4118485daf`: claims 1 and 5 are verified,
claim 2 is falsified from the primary table, and claims 3 and 4 remain
inconclusive because the judge requires independently executed CelebA and
molecular results. The public logbook has since been cleaned at revision
`23a5e0bd05cdef4a8996d7ae4c0a2fbd1515a92d`. The campaign continues until all
recoverable claims have executed evidence; a failed route advances to another
materially different route rather than ending the campaign.

| # | Materially different route | State | Evidence / decision |
|---:|---|---|---|
| 1 | Audit the official author repository for released checkpoints | exhausted | `TanjaBien/LogDiff@94ef35b` contains checkpoint paths but no weights. |
| 2 | Inspect author Git branches, releases, issues, forks, history, and unreachable objects | exhausted | One branch, no releases/issues/forks, three commits, no unreachable checkpoint objects. |
| 3 | Search Hugging Face models, datasets, and Spaces by title, author, and CMNIST identifiers | exhausted for author weights | No author LOGDIFF checkpoint; unrelated MNIST checkpoints were evaluated separately. |
| 4 | Recover the independent AutoSOTA paper-scale run | partial lead | Its public report says CMNIST N=2 reached 92.04%, but the checkpoint/raw cache and cited experiment commit are not public, so the report is not used as executed evidence. |
| 5 | Train the full official CMNIST diffusion U-Net locally | infeasible at paper batch, not abandoned globally | Full model benchmarked on MPS: batch 128 takes 6.26 s; batch 256 reaches the 7.7 GiB safety cap; paper uses batch 1,024 for 50,000 updates. A cosmetic small-batch run is rejected. |
| 6 | Use a public trained class-conditional MNIST DDPM on real images | active | `Shiroi-max/ddpm-mnist-conditional-32@368673b`; 1,708,289 parameters; labels 0-9; 50-step MPS smoke produced correct class-conditioned digits. |
| 7 | Train the paper's noise-aware ResNet-18 probability estimator at full data/epoch scale | complete | Official two-head architecture, all 60,000 ColoredMNIST examples, batch 1,024, 50 epochs. Both composition and independent judge classifiers completed; the judge checkpoint hash is `ff8dbb5253278ad46e0033959045f9558927b0e814cd04f98fc747d6629adfb6`, with full-test digit/colour accuracies `0.9814/1.0`. |
| 8 | Literal primary-source falsification of anchored Table-2 ranges | complete | Table 2 has LOGDIFF 85.1-94.4%, not 94-98%, and constant 57.9-76.1%, not 63-77%, across the quantified dataset/operator cells; the official judge marked the claim falsified. |
| 9 | Exhaustive Proposition C.2 certificate for independent categorical groups | complete | Every nonconstant event in multiple product spaces was compiled into CI conjunctions plus ME disjunctions and matched direct differentiation; officially verified. |
| 10 | Exhaustive Proposition C.2 certificate for nested/ME taxonomy predicates | complete | Every nonconstant event over an eight-leaf taxonomy matched, and an overlapping non-nested control was rejected; officially verified. |
| 11 | Recover the paper's exact CelebA checkpoints from author and public mirrors | exhausted | Exact filenames, author accounts, repository objects, GitHub code, Hugging Face models/datasets/Spaces, and an AutoSOTA copy were checked; no executable checkpoint or raw run was available. |
| 12 | Execute a real-data CelebA negation experiment with a public pretrained DDPM | active | Pin `shalpin87/diffusion_celeba@91d0ff0`, download official CelebA, train the released noise-aware two-head classifier, and compare constant negation with LOGDIFF dynamic odds using clean-fid. This is independently executed evidence, explicitly not claimed as the paper's missing 64px checkpoint. |
| 13 | Recover molecular data, target indices, weights, and docking tools | complete | DualDiff data and three weight files were recovered and hashed; GRM5 index 29 and RRM1 index 371 were identified; local AutoDock Vina reference docking passed. |
| 14 | Port the released molecular stack to Apple MPS without changing CPU semantics | active | Replaced unsupported graph operations through a tested native helper, removed a hard-coded `/scratch` ligand path, and added PyG parity and metric tests. |
| 15 | Reimplement the missing LOGDIFF molecular composition layer from the paper equations | active | Added posterior estimation plus exact AND/AND-NOT score and categorical-logit rules to the recovered FKC pipeline. This is labeled as a paper-derived reconstruction, not author-source parity. |
| 16 | Execute the actual 32-ligand GRM5/RRM1 protocol and independently dock both targets | active | Run LOGDIFF AND and AND-NOT generation at 1000 steps, beta 2.0, 23 atoms and 32 trajectories; evaluate both proteins with Vina, then report product and target-separation metrics with per-run artifacts. Each such condition/seed is one of the paper's eight experiments. |
| 17 | Exercise generated-ligand docking and pocket alignment before the full campaign | complete smoke gate | A 32-sample, one-step portability batch yielded one reconstructable 23-atom ligand. After narrow RDKit-six, out-of-grid score-only, and PDBQT-pose compatibility repairs, full Vina docking completed against both proteins and pocket alignment passed (23 mapped atoms, RMSD 0.559 A). This is a pipeline gate only, not claim evidence; the denoising scale remains 1/1000 and only one ligand was valid. |
| 18 | Vectorize equal-size MPS k-nearest-neighbour graphs | partial optimization | The actual 32 x 441-node construction fell from about 0.50 s to 0.28 s with identical PyG edge sets, but a full LOGDIFF step remained about 120 s because the nine-layer neural forwards dominate. Preserved as a portability improvement, not treated as sufficient. |
| 19 | Fuse the two current-condition and two Appendix-D.3 probe forwards | exhausted for speed | Fused and separate one-step outputs matched exactly for positions, categorical states, trajectories and posteriors, but fused CPU wall time was 4.44 s versus 4.29 s separately. The option remains for parity testing and is disabled in the campaign. |
| 20 | Run half-precision inference on Apple MPS | exhausted for speed | After repairing mixed-dtype inputs, FP16 executed correctly but took 18.43 s for four trajectories versus 15.62 s in FP32 under the same shared load. Full runs remain FP32. |
| 21 | Compile the CPU neural forward while graph-breaking custom KNN/scatter ops | partial optimization | Direct compilation failed on the `torch-cluster` fake-tensor boundary. Explicit graph breaks made it execute; cached steady-state one-ligand steps improved from roughly 4.3-4.9 s to 3.7-4.1 s without changing outputs. |
| 22 | Shard the full 32-ligand protocol into restartable exact trajectories | active | An idempotent runner uses beta 2.0, 1000 steps, 23 atoms, one exact Appendix-D.3 Monte Carlo probe and up to ten retries per sample. Four-worker 20-step gate: 4/4 complete, 80/80 finite steps, all artifacts hashed. Eight workers caused swap and lower aggregate throughput, so four is the measured host optimum. |
| 23 | Chain complete generation, hash-validated merging, full Vina docking, and condition-separated reporting | complete for seed 0 | Both 32 x 1,000-step seed-0 conditions are generated, merged, fully evaluated, separately summarized, and jointly reported. The independent auditor accepts both as full provenance-complete experiments. AND has mean paired product `59.57176296875`; AND-NOT has mean directed separation `0.6097096774193547` over 31 valid-unique ligands, with one deterministic disconnected-fragment rejection retained as a null row. |
| 24 | Independently bind metrics to provenance and falsifying controls | complete for the four-ligand checkpoint; prepared for complete runs | `audit_molecular_campaign_evidence.py` validates source/config hashes, every shard hash, merged-tensor protocol, exact target identities, and docking-result hashes; canonicalizes SMILES; recomputes Table 5/6 metrics without pandas; applies pair-breaking and target-swap controls; keeps AND/AND-NOT separate; and refuses full readiness before 8 x 32 ligands per condition. Refreshed checkpoint report SHA-256: `f738b96ef767daf8e45abf40e494991965a1226adf9bb84a1e56d7799c5b925a`. |
| 25 | Continue paper experiments 2--8 without competing with the live shared chain | prepared, dry-run only | A deterministic manifest schedules the 14 missing seed/condition campaigns with exclusive paths, seed-0 write protection, exact source/protocol/weight hashes, four exact OAM/CVM/qIO process blockers, a global heavy-process scan, a 4 GiB memory gate, failure-isolated dependencies, and a ten-material-approach escalation registry. The static audit validates 14 campaigns and 72 commands but correctly refuses launch while PIDs 476, 40427, 41276, and 41861 remain. See `docs/MOLECULAR_REPLICATE_CONTINUATION.md`. |
| 26 | Move the fourteen missing molecular experiments to a resumable Colab CUDA handoff | prepared, not executed | A deterministic no-seed-0 input bundle pins Colab 2026.04, PyTorch 2.10 CUDA 12.8, matching PyG extensions, every source/vendor/weight hash, and fourteen exclusive condition/seed paths. Execution fails closed until the real GPU passes the CUDA kernels, strict FP32 checkpoint load, one-step compiled forward, and 20-step timing gates. Validated samples checkpoint atomically to Drive; an independent local verifier binds all 448 raw shards, merges, full Vina outputs, reports, and audits. This preserves equations and protocol but explicitly does not claim bitwise CPU parity or a hardware-homogeneous eight-run aggregate. See `docs/MOLECULAR_COLAB_CUDA_HANDOFF.md`. |
| 27 | Prepare a final-only content-addressed C4 import and existing-Space repair | prepared, not executed | The terminal gate rejects seed-0-only evidence and requires all sixteen condition/seed campaigns, 8 x 32 ligands per condition, full Vina, exact provenance, condition separation, and zero independent-audit blockers. Import reruns the auditor and requires byte-identical output; Space preparation refetches the exact verdict and existing parent, and apply requires separate literal root approval. No terminal publication has been performed. See `docs/C4_TERMINAL_HANDOFF.md`. |
| 28 | Run seeds 1-7 as disjoint, resumable HF `cpu-upgrade` generation slices | statically prepared, not launched | A contract-bound CPU runner rejects seed 0 and partitions every condition/seed campaign into four exclusive eight-sample, six-hour slices. Each slice has a unique contract-addressed bucket prefix, cumulative attempts, payload hashes, sample SUCCESS-last, slice SUCCESS-last, exact source/input/environment binding, and a launcher that blocks only an exact active OAM condition/seed duplicate while preserving unrelated Jobs. A local content-addressed importer requires all 56 slices/448 samples and rejects partial or mixed identities. The route requires its own paid preflight and per-slice shared-ledger reservations; its worst-case generation ceiling is USD 10.11 including preflight. See `docs/OAM_HF_CPU_REPLICATES.md`. |

No route above is treated as proof of impossibility. If either active execution
fails, the failure is preserved as evidence and the next materially different
route is attempted. Authorized HF T4 and `cpu-upgrade` compute remains governed
by the shared USD 40 aggregate cap, exact per-Job reservations, immutable output
namespaces, and nonduplication gates.
