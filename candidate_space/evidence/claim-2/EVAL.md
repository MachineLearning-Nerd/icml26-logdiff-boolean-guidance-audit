# Claim 2 evaluation contract

`summary.json` must report `claim_2.status = FALSIFIED`, with 2/8 LOGDIFF cells
and 5/8 constant cells inside their claimed intervals. The range-conforming
negative control must be rejected. Any discrepancy causes a nonzero exit.

No generated images are used: the tested statement is contradicted by the
paper's own exact quantified table cells.
