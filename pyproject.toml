[project]
name = "logdiff-reproduction"
version = "0.1.0"
description = "Claim-by-claim reproduction of LOGDIFF"
requires-python = "==3.12.*"
dependencies = [
  "numpy==2.3.2",
]

[dependency-groups]
dev = [
  "pytest==8.4.1",
]

[tool.uv]
package = false
